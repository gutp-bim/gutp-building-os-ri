global:
  # 30s is sufficient for KPI dashboards and reduces Prometheus TSDB storage by ~50% vs 15s.
  # Increase to 15s if you need sub-30s granularity in Grafana.
  scrape_interval: 30s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/recording_rules.yml

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: [localhost:9090]

  # otel-collector self-metrics (pipeline/exporter health). Application metrics
  # arrive via remote-write from the collector, not by scraping the apps.
  - job_name: otel-collector
    static_configs:
      - targets: [building-os.otel-collector:8888]

  - job_name: nats
    static_configs:
      - targets: [building-os.nats:8222]
    metrics_path: /metrics

  - job_name: minio
    metrics_path: /minio/v2/metrics/cluster
    static_configs:
      - targets: [building-os.minio:9000]

  - job_name: postgres
    static_configs:
      - targets: [building-os.postgres-exporter:9187]
