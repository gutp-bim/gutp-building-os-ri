apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gateway-bridge-reference
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    notifications.argoproj.io/subscribe.on-sync-failed.slack: "building-os-alerts"
spec:
  project: default

  source:
    repoURL: https://github.com/gutp-bim/gutp-building-os-ri.git
    targetRevision: HEAD
    path: kubernetes/helm/gateway-bridge
    helm:
      valueFiles:
        - ../../argocd/values/gateway-bridge-reference.yaml
      values: |
        image:
          repository: harbor.example.com/building-os/building-os-gateway-bridge

  destination:
    server: https://kubernetes.default.svc
    namespace: building-os

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 3
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m

  revisionHistoryLimit: 5
